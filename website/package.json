{
  "name": "doze-website",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "The doze documentation site — doze.nerdmenot.in",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/gen-engines.mjs && astro build",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=${CLOUDFLARE_PROJECT:-doze-docs}"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.36.0",
    "astro": "^5.6.1",
    "sharp": "^0.34.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
