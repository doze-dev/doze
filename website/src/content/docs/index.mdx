---
title: doze
description: docker-compose for local development, without the virtualization. Real engines as native processes — your machine stays cool, your debugger just attaches.
template: splash
hero:
  title: Your machine is powerful. Development should feel like it.
  tagline: >-
    doze is docker-compose for local development, without the virtualization.
    The moment a VM enters the picture your fans spin up, your RAM is spoken
    for, and your debugger is on the wrong side of a boundary. doze runs
    Postgres, Valkey, a Mongo-compatible store, local S3/SQS/SNS — real,
    unmodified engines — as native processes on your machine. Declared in one
    file, booted when something connects, asleep when you walk away.
  image:
    file: ../../assets/logo.png
  actions:
    - text: Get started
      link: /start/getting-started/
      icon: right-arrow
    - text: Why doze?
      link: /why/doze/
      variant: minimal
---

import { Card, CardGrid, Code } from '@astrojs/starlight/components';

export const heroHcl = `# doze.hcl — your whole local backend, declared
postgres "app"   { version = 18 }
valkey   "cache" { version = 9 }

s3 "uploads" {
  port       = 9000
  versioning = true
}`;

export const heroSh = `$ brew install doze-dev/tap/doze
$ doze up
  ✓ app (postgres 18) ready   ✓ cache (valkey 9) ready   ✓ uploads (s3) ready
$ doze status
  ● app     postgres 18   active   127.0.0.1:5432   42.5M
  ○ cache   valkey 9      asleep   —                —      # zero cost until used`;

<div style="max-width: 46rem; margin-inline: auto;">
  <Code code={heroHcl} lang="hcl" title="doze.hcl" />
  <Code code={heroSh} lang="shellsession" title="terminal" />
</div>

## The point

Containerization is great — for shipping. For *development*, convenience made
it acceptable to be resource-hungry: a VM holding half your RAM all day, the
whole stack running while you edit one service, remote-k8s dev loops with
latency where your inner loop should be. doze reverses that bargain.

<CardGrid>
  <Card title="Your fans stay off" icon="moon">
    No VM, no reserved slab of RAM. At rest, your entire backend is one ~15 MB
    daemon; engines boot on first connection and reap to zero when idle. The
    machine's power goes to your build, not to virtualization.
  </Card>
  <Card title="Your debugger just attaches" icon="magnifier">
    Everything is a native process on your kernel. `dlv`/`lldb` attach
    directly, `ps`/`lsof`/Instruments see the real engine, core dumps land on
    your disk. No boundary, no port-forward mazes, no bind-mount heisenbugs.
    [The debugger story →](/why/not-containers/)
  </Card>
  <Card title="Real engines, exactly" icon="approve-check">
    The actual upstream Postgres 18, the actual Valkey — not images, not
    emulations. Every extension and wire feature behaves like production,
    pinned to the byte in `doze.lock` for your whole team.
  </Card>
</CardGrid>

## Pick your path

<CardGrid>
  <Card title="Use doze" icon="rocket">
    Install it, declare your backend, get on with your app.
    [Install →](/start/install/) · [Getting started →](/start/getting-started/) ·
    [Coming from docker-compose →](/start/from-docker-compose/)
  </Card>
  <Card title="Understand it" icon="open-book">
    The full case: native processes vs containers, why HCL, and an honest look
    at the alternatives.
    [Why doze →](/why/doze/)
  </Card>
  <Card title="Build a module" icon="puzzle">
    Every engine is a plugin, and third-party modules are exactly as capable
    as official ones. Start from a working template.
    [Author guide →](/modules/overview/)
  </Card>
  <Card title="Run a registry" icon="setting">
    Modules arrive signed, key-pinned, and locked — and the registry is static
    files you can host yourself, air-gapped included.
    [Operator guide →](/operate/trust-architecture/) ·
    [Browse modules →](/registry/)
  </Card>
</CardGrid>
